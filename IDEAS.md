# 功能想法记录（IDEAS）

> 这是**想法池**，不是路线图。里面大部分东西不会做，写下来是为了不重复讨论。
> 每条都标注了**数据可行性**——有些看着美好，但数据根本拿不到。

当前版本：v1.1.2。

## 插件定位

一句话：**一眼看清一款 Steam 游戏值不值得买、现在有多少人在玩。**

判断新功能要不要做的标准：
1. 是否服务于「买之前想知道什么」或「现在玩的人多不多」
2. 是否**不需要 API Key**（这是本插件相对同类的主要卖点）
3. 是否能在国内网络环境下稳定拿到数据

不满足这三条的，即使技术上做得到，也会让插件变差。

---

## 一档：贴合定位，数据已确认可拿

### 1. 价格提醒 / 愿望单监控 ⭐ 优先级最高

**问题**：现在插件是「用户主动问」。但打折这种事，用户真正要的是「别让我错过」。

**做法**：用户订阅若干游戏 → 定时轮询 → 折扣出现、或价格跌破史低时主动推送。

**数据**：
- 折扣与结束时间：`IStoreBrowseService/GetItems` 的 `best_purchase_option.active_discounts[].discount_end_date`（已有）
- 史低：小黑盒历史价格接口（已有）
- 促销列表：`store.steampowered.com/search/results/?specials=1`（已有）

**结论**：数据**全部已在手**，不需要新接口、不需要 Key。这是唯一能把插件从「查询工具」变成「有用工具」的功能。

**风险**：轮询频率要克制；需要持久化订阅列表；推送目标需要 session 标识。

### 2. 商店链接 / 一键跳转 ✅ 已在 v1.3.0 完成

`GameCard.store_url` 与 `DealItem.store_url` 此前存在但**只是灰色小字**，容易被忽略。
现在详情卡显示为「商店链接：」+ 高亮 URL。

### 3. 详情卡补充：简介、发行日期 ✅ 已在 v1.3.0 完成

`short_description` 与发行日期此前已取回但**从未渲染**，现已在详情卡显示（简介最多 3 行）。

仍可考虑：`assets` 里的游戏截图。当前只用了封面。

注意：`genres`（类型）**`GetItems` 不返回**（实测现有请求与 `include_genres` 都没有）。
但 **`appdetails` 返回**，小黑盒搜索接口也返回，见下面的「新发现」。

### 4. 距史低差额提示 ✅ 已在 v1.3.0 完成

现在详情卡右侧显示「距史低还差 ¥24」，已是史低时显示「当前已是史低」。

---

## 二档：可行但要权衡

### 5. 多区域比价（国区 / 阿区 / 土区）

`GetItems` 支持 `country_code`，技术上直接可做。

**但**：跨区购买**有封号风险**。不建议做，至少不该由插件引导用户去做。

### 6. 好友 / 群内游戏对比（「我们能一起玩什么」）

需要 Steam Web API Key + 用户公开资料。

**与「无需 Key」卖点冲突**，暂不考虑。

### 7. 游戏时长 / 成就

同上，需要 Key 且需用户授权。

---

## 三档：建议不做

| 想法 | 不做的原因 |
| --- | --- |
| Nintendo 价格查询 | 用户已明确说不做；且**真正的史低数据拿不到** |
| SteamTools(Watt Toolkit) 代理 | Avalonia/.NET 桌面 GUI，塞不进 headless 容器；且问题已被 `api.steamchina.com` 解决 |
| 全站游戏在线人数绝对排名 | 需要逐个查几十万款游戏，不现实 |
| 游戏攻略 / 新闻聚合 | 偏离定位，且数据源不稳定 |

---

## 稳定性欠账（比新功能更该做）

功能面已经覆盖得差不多了。真正的风险在**依赖的都是非官方接口**：

| 依赖 | 影响 | 现状 |
| --- | --- | --- |
| 小黑盒搜索接口 | 口语化中文名（`泰拉瑞亚`）主要靠它 | 已有 Steam 搜索并行兜底 |
| 小黑盒史低接口 | 史低是核心卖点之一 | 已有 `_safe_lowest` 兜底 |
| `IStoreBrowseService` | Steam 未公开文档接口，可能改字段 | 有主机故障转移 |
| 热门榜接口 | 同上 | 有失败返回 |

**核心思路**：功能少是 60 分，核心链路挂掉是 0 分。
目标不是「永不失败」，而是「**失败时体面降级并说人话**」。

已做：
- 史低失败 → 卡片照出，只是不显示史低
- 单个游戏人数失败 → 跳过该游戏，不影响整张榜
- 封面下载失败 → 画占位图
- [x] 小黑盒搜索失败时的友好提示与降级路径（v1.2.0：改用 Steam 搜索兜底）
- [x] 接口自检 / 健康探测（v1.2.0：启动时后台探测并写日志）

---

# 新发现（2026-09-20 全量探测）

把「免费、无需 Key」的数据源挨个实测了一遍。**下面是测出来的，不是猜的**，
每条都附了实测值。分档依据是「数据是否真的拿得到」而不是「听起来好不好」。

## A. 已经拿得到、只是没用的字段 ⭐ 性价比最高

### A1. 小黑盒搜索其实返回 **37 个字段**，我们只用了 3 个

现在只解析了 `name` / `steam_appid` / `follow_num`。实测同一份响应里还有：

| 字段 | 实测值（艾尔登法环 / 泰拉瑞亚 / 黑神话悟空） | 价值 |
| --- | --- | --- |
| `score` | `9.6` / `9.8` / `9.8` | 小黑盒玩家评分 |
| `genres` | `['动作','角色扮演']` / `['动作','冒险','独立']` | **类型**，正好补上 `GetItems` 不给的洞 |
| `core_tags_str` | `开放世界` / `沙盒类` / `ARPG` | 核心标签，一行就能说清是什么游戏 |
| `online_player` | `5万` / `5万` / `2万` | 在线人数（已取整） |
| `follow_num` | `1720006` / `758938` / `1843725` | 关注数，可当热度信号 |
| `price.lowest_price` / `lowest_discount` | `179`/`40%`、`12`/`67%`、`188`/`30%` | 史低与史低折扣（另一条史低来源，可交叉校验） |
| `release_date_desc` | `2022年02月25日` | 发售日期 |
| `has_achievement` | `1` | 有无成就 |
| `platforms` | `['steam']` | **平台**，见 A2 |
| `heybox_price.coupon_info` | `领券减10` / `领券9折` | 小黑盒商城优惠券 |

**几乎零成本**：这些字段本来就在响应里，只是没解析。加「类型 / 标签 / 小黑盒评分」
到详情卡，不需要任何新请求。

**不能用的**（实测排除，别再试）：
- `peak_user_num` → 样本里**全是 `N/A`**
- `owned_rate` → 样本里**全是 `0`**

### A2. `platforms` 是合成 appid 的第二个独立信号

修「崩坏3 查不到」时，那条合成条目的 `platforms` 实测是 **`['epic']`** ——
也就是说小黑盒自己就标注了「这不是 Steam 游戏」。这独立于 `>= 9e8` 的 id 阈值。
可以作为加固：要求 `platforms` 含 `steam`。（当前按 id 过滤已够，此为双保险。）

## B. Steam 侧免费可用的接口（全部实测通过，均无需 Key）

| 接口 | 实测结果 | 用途 |
| --- | --- | --- |
| `search/results/?maxprice=free&specials=1` | **4 条**限时免费 | **喜加一提醒**，见 C1 |
| `search/results/?filter=globaltopsellers` | 9054 条，前 3 = Steam Frame / WARDOGS / EA FC 27 | 热销榜 |
| `appdetails` | **39 个字段**：`genres`、`metacritic=94`、`supported_languages`、`screenshots=10`、`dlc=3` | 详情卡扩充，见 C2 |
| `saleaction/ajaxgetdeckappcompatibilityreport` | `3`=已验证 / `2`=可玩 / `1`=不支持 | Steam Deck 兼容，见 C2 |
| `ISteamNews/GetNewsForApp/v2` | 可用，但内容**是英文** | 更新公告，见 D1 |
| `GetGlobalAchievementPercentagesForApp/v2` | 42 个成就，最稀有 10.4% | 成就稀有度，见 D2 |
| `featuredcategories` | `top_sellers`=10、`new_releases`=30、`specials`=10 | 备用榜单源 |

**实测拿不到的**（别再试）：
- `steamstat.us/status.json` → **403**，被 Cloudflare 挡（返回 "Just a moment..."）
- `ISteamWebAPIUtil/GetServerInfo` → 只有 `servertime`，**没有状态信息**
- 结论：**Steam 服务器状态做不了**，没有可用的免费数据源。

---

## C. 一档：建议优先做

### C1. 限时免费领取（喜加一）✅ 已在 v1.5.0 完成

命令 `steam免费` / `steam限免` / `steam喜加一` / `steam限时免费`。

**问题**：限时免费是**最容易错过**的东西，而且错过就没了。这比打折更急。

**数据**：`search/results/?maxprice=free&specials=1`，**实测 4 条，且全部是真·喜加一**：

```
2484180  Deadshot                  原价 ¥22.00 → ¥0.00  -100%
2000040  Space Menace              原价 ¥29.00 → ¥0.00  -100%
 447700  Crystal Crisis            原价 ¥70.00 → ¥0.00  -100%
4355480  《消逝的光芒：困兽》武器包    原价 ¥20.00 → ¥0.00  -100%
```

「本来就免费」的条目：**0 条**。所以 `specials=1` 已经过滤干净了。
无需 Key，无需新接口。

**做法**：`steam喜加一` / `steam限免` → 卡片列出游戏名、封面、原价、结束时间。

**为什么排第一**：
- 完全贴合「买之前想知道什么」，而且**比价格提醒更急**（价格提醒用户已明确说不做订阅）
- **不需要订阅机制**：用户主动问即可，避开了订阅列表持久化和推送目标的复杂度
- 复用现有打折卡的渲染，改动小

**待验证**：样本里混进了 **DLC**（第 4 条是《消逝的光芒：困兽》的武器包），
需要按类型过滤只留本体 —— 这一条**动手前要验证**。
另外 4 条是**当前时刻**的样本，数量随促销变化，别把「4 条」当成常态。

### C2. 详情卡补「中文支持 + Steam Deck 兼容」

对国内用户，这两个是买之前最实际的顾虑。

**数据**（均实测通过）：
- `appdetails.supported_languages` 里 `<strong>*</strong>` 标记**完整中文语音**
  （实测艾尔登法环 = True）
- Deck 兼容性：艾尔登法环=**已验证**、空洞骑士=**已验证**、CS2=**可玩**、
  黑神话悟空=**不支持**（映射已确认）

**代价**：`appdetails` 是**逐个请求且有速率限制**——本次开发中实测批量请求
直接返回空 body，需要小批量 + 退避。必须加缓存，否则会拖慢卡片。
（本次验证喜加一时又复现了一次：6 个 appid 批量请求直接返回非 JSON，
连 `is_free` 都读不到。这个限制是真实存在的，不是偶发。）

### C3. 类型 / 标签 / 小黑盒评分（见 A1）

零新请求，直接从已有响应里解析。建议加进详情卡：
「动作 · 角色扮演 · 开放世界 · 小黑盒 9.6」。

## D. 二档：有价值但要权衡

### D1. 游戏更新公告 `steam更新 <游戏>`

`ISteamNews` 实测可用、无 Key，但两个问题：
1. 内容**是英文**（传 `l=schinese` 也没本地化）
2. 返回偏**新闻稿**而非「更新日志」
→ 需要先验证实际内容质量，再决定做不做。

### D2. 成就稀有度

`GetGlobalAchievementPercentagesForApp` 实测可用、无 Key（42 个成就，10.4%~75%）。
但定位偏「玩之后」，与「买之前」关系弱。折中做法：详情卡只显示**成就数量**，
不展开稀有度。

### D3. 小黑盒商城优惠券

实测 `heybox_price.coupon_info` 会给出「领券减10」「领券9折」这类**真实省钱信息**，
国内可用。但要谨慎：这等于**引导用户去小黑盒商城下单**，属于商业推广，
需要先问清楚再决定。

## E. 三档：建议不做

| 想法 | 不做的原因 |
| --- | --- |
| Steam 服务器状态 | **数据拿不到**：steamstat.us 被 Cloudflare 挡（403），官方接口只有时间 |
| Jump 的价格 / 史低 | Jump **无公开 API**，需逆向其 App 私有接口。与「稳定可维护」冲突，且史低已有小黑盒 |
| 促销日历 / 下次大促时间 | `steamsales` 页面存在（实测 200、1MB）但结构未验证；大促日期通常只有 SteamDB 有，非官方 |
| `peak_user_num` / `owned_rate` | **实测字段是空的**（全 N/A / 全 0） |

## F. 一句话结论

**最划算的是 C1（喜加一）和 C3（类型/标签/评分）**：
前者是唯一「急」到值得单独做命令的需求，后者几乎零成本。
C2 价值高但要先解决 `appdetails` 的速率限制。
