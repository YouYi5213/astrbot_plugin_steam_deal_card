# Changelog

## 1.4.5

- **修掉「标题中间插字」的游戏查不到**。用 71 个中文游戏名做了全量排查（见下），
  发现 `荒野大镖客2` 仍然失败，但根因和 1.4.4 完全不同：小黑盒**给对了** appid
  `1174180`，名字是官方全称 `荒野大镖客：救赎2`，可用户输入的是口语简称
  `荒野大镖客2`。中文没有词边界，官方名在中间多了「救赎」两字，于是前缀、
  子串、分词三种匹配全部落空，**得分 0 被当成无关候选丢掉**。
  现在增加一档「按顺序跳字匹配」（60 分，低于前缀 85 分），并加了三道限制：
  短的一方必须含中文、长度至少 3、且要覆盖长的一方一半以上。
  这样 `荒野大镖客2` 能命中，而 `幻塔` 不会去匹配 `Fantasy Grounds - The Tower
  of Jhedophar`，`gtav` 也不会匹配 `Grand Theft Auto V`。
- **71 个中文游戏名排查结果**：55 个直接出卡、2 个给出候选列表
  （`英雄联盟` 列出的是真实的外传系列、`全面战争` 本身就是一个系列，都合理）、
  14 个查不到，且逐个到 Steam 复核确认**确实不在 Steam 上**
  （原神、绝区零、塞尔达、宝可梦等；`蔚蓝档案`、`幻塔` 只有无关的 DLC）。
  也就是说这个样本里没有残留的错误「查不到」。

## 1.4.4

- **修掉「崩坏3」查不到**。根因是小黑盒会给**没有 Steam appid 的游戏发自己的
  合成 id**：`崩坏3` 在 Steam 上是 `1668940`，小黑盒给的是 `900045980`。
  插件把这个合成 id 当成 Steam appid 去查，Steam 当然查不到，候选被丢弃后
  就报「没有找到」。实测 137 条 PC 记录：带真实 `appid` 字段的全部有效，
  没有该字段的 30 条全是合成 id（抽样 12 条无一存在于 Steam），因此按
  `>= 9e8` 过滤。
- **中文名解析改为两个接口合并**。过滤掉合成 id 后小黑盒就查不到 `崩坏3` 了，
  但 Steam 自己的搜索接口认得官方中文标题（实测 `崩坏3`、`巫师3`、
  `黑神话：悟空`、`艾尔登法环`、`赛博朋克2077`、`双人成行` 都命中），
  两个接口各能解决对方解决不了的名字，所以现在**两个都查、合并后统一打分**。
  此前只在 Heybox 挂掉时才用 Steam 搜索，白白丢掉了这条解析路径。
- **搜索接口加了站点兜底**。`/api/storesearch/` 只存在于国际站，而这台服务器
  到国际站是**整段时间不可达**的（实测 15 秒超时、返回码 000），国内站该接口
  又是 404，于是 `崩坏3` 在服务器上依旧查不到——本地能查到只是因为本地网络好。
  现在会回退到 `/search/results/`，该接口**两个站都提供**：实测国际站挂掉时，
  国内站仍能返回 `崩坏3` → `1668940`。这个接口的索引还更宽（`只狼`、
  `空洞骑士` 都能查到，而 `/api/storesearch/` 查不到）。
  另外「查无结果」与「查不动」现在区分开了：接口正常返回空才算没有这个游戏。
- **修正文档里一处想当然的描述**。此前写「Steam 商店搜索接口不识别中文」，
  实测只对简称成立（`泰拉瑞亚`、`只狼`、`空洞骑士` 返回 0 条），官方全称是认的。
  这个过于绝对的假设，正是上面那个 bug 一直没被想到的原因。
- **修掉详情卡遇到含换行简介直接崩掉**。`崩坏3` 的 `short_description` 带 `\r\n`
  （这个字段来自 `GetItems`，与 `appdetails` 的 `short_description` 不是同一份），
  而 Pillow 拒绝测量含换行的文本（`ValueError: can't measure length of
  multiline text`），于是渲染抛异常。现在测量前统一把空白折叠成单个空格。

## 1.4.3

- **移除 `steam热门新品` 命令**（含 `steam新品`、`steam新游` 别名）。
  上一版换成首页货架后仍然不对，根因是**首页那个货架根本不是热度榜**：
  实测它严格按发售日期排列（9/18、9/18、9/18、9/17、9/17…），
  与 `/explore/new/` 的「热门新品」标签页是两份不同的列表
  （集合重合仅 19/30，explore 页面第 2 位的《夜之海》首页货架里完全没有）。
  换言之它是「最新发售」，把它当热度榜展示本身就是错的，因此撤掉而不是再换数据源。
- **`steam即将推出` 保留并更名为「热门即将推出」**，与商店首页文案一致。
  这个货架经逐条核对确实可用，发售日期也保留。
- 顺带清掉随之失效的代码：`popular_new`、`home_shelves`（改为只取单个货架的
  `home_shelf`）、`parse_explore_tab_items` 及其正则、`STEAM_EXPLORE_NEW_URL`。

## 1.4.2

- **`steam热门即将推出` 质量大幅提升**。此前用的 `featuredcategories.coming_soon`
  只有 10 条且全是小制作与 Demo（DIY Dadish、Death Counter Demo）。改用商店
  首页「热门即将推出」货架后是 30 条真正有份量的未发售游戏，并带**发售日期**：
  控制: 共振、SILENT HILL: Townfall、守墓人2、EA SPORTS FC 27、Happy Wheels。
- 首页货架**内嵌在首页 HTML 里**，不需要额外接口，因此直接解析。
- 首页请求加站点兜底：`store.steampowered.com` 从大陆服务器上会整段时间连不上
  （实测 000/12s），此时改用 `store.steamchina.com`（200/1.9s，同结构）。
  国内站商品库小得多，因此仅在国际站失败后使用，卡片上会注明。
- 卡片新增「发行日期」一行（`steam热门即将推出` 使用）。

## 1.4.1

- **修正 `steam热门新品` 取错了榜**。上一版用的商店搜索 `popularnew` 排序是
  **总人气**排序，返回的是 WARDOGS、CS2、PUBG、GTA V 这类长期在线的老游戏，
  根本不是新品。现在改为照实解析商店「热门新品」页面
  （`store.steampowered.com/explore/new/`），得到的是花砖公司、夜之海、
  收冠之战、无尽帝国 2 这样真正的新游。
- 说明这个榜为什么不能用搜索接口代替：把 `newreleases`、`popularnew`、
  `Reviews_DESC`、`Released_DESC` 等参数组合在两个站点上逐个试过，与店铺页
  的重合度是 **0/10**。该榜只存在于页面 HTML 中，因此页面不可达时会退回搜索
  结果，并在卡片上注明「以下为搜索接口的近似结果」。
- 补充实测结论：`store.steampowered.com` 与 `api.steampowered.com` 的连通性
  **互相独立且会各自中断**（同一时刻前者 200/2.1s、后者超时 12s），上一版
  记成"整段不可达"并不准确。

## 1.4.0

- **新增 `steam热门新品`**，列出 Steam 热门新品榜（可跟数字指定条数）。
  数据源是商店搜索的 `popularnew` 排序，而不是 `newreleases`：后者按发售日期
  返回 GTA V、Apex Legends 这类老游戏，并不能当"新品"用。
- **新增 `steam即将推出`**，列出 Steam 官方「即将推出」货架。
  说明：Steam **没有**为未发售游戏提供热度排序 —— `comingsoon` 搜索按发售日
  期排序，实测前 100 条无一条有评测，且全是 Demo 与小制作。这里用的是商店
  首页货架（`featuredcategories`），是 Steam 自己策展的结果，质量更可靠。
- **商店搜索接口增加站点兜底**。`store.steampowered.com` 在国内服务器上会整段
  时间连不上（实测容器与宿主机同时超时，商店与 API 均不可达，而百度 / GitHub /
  Steam 静态资源均正常），而 `store.steamchina.com` 同一路径返回同样的 JSON
  结构。现在两个站点依次重试；国内站商品库小得多（popularnew 13 条 vs 369 条），
  因此只在全球站点失败后才使用。
- **「即将推出」增加搜索兜底**。货架接口只有全球站点提供，不可达时改用
  `comingsoon` 搜索（排除 Demo）顶上，宁可给弱一些的数据也不返回空。
- **免费游戏不再被榜单丢掉**。热门榜上 CS2、PUBG、Apex 这类免费游戏原本会
  因为"没有价格"被过滤掉，而它们恰恰是玩的人最多的。
- **修正免费游戏的史低显示**。小黑盒对免费游戏会返回同 IP 付费版本的史低
  （CS2 显示成"史低 96"），现在免费游戏一律显示"史低 暂无"。
- **修正未发售游戏的评价显示**。原本渲染成"无用户评测 · 0% 好评 · 0 篇"，
  读起来像差评，现在显示"暂无用户评测"。

## 1.3.4

- **移除卡片底部的商店链接**。链接已随消息以文本发出（那里才能点击），
  再画在图片上只是重复一行**谁也点不了**的文字。卡片现在以简介收尾，
  高度相应减少约 50px。

## 1.3.3

修正 1.3.2 发出的错误链接。**之前的小黑盒链接指向的不是那个游戏。**

1.3.2 用的是 `/app/topic/game/{appid}`，实际打开是无关页面。
该站点是单页应用，**服务端对任何 appid 都返回同一个 2658 字节外壳**，
所以「HTTP 200」根本不能证明链接有效 —— 这正是当初漏掉它的原因。

正确路径需要 **`/pc/` 段**：

```
https://www.xiaoheihe.cn/app/topic/game/pc/{appid}
```

依据（第三方索引的真实页面地址，而非猜测）：

- 泰拉瑞亚 → `.../app/topic/game/pc/105600`
- 星露谷物语 → `.../app/topic/game/pc/413150`

两个 appid 都与该模板吻合。已加测试锁定 `/pc/` 段，防止再次漏写。

## 1.3.2

两处体验修正。

- **图片与链接合并为同一条消息**：1.3.1 把链接作为独立消息发出，
  结果一次查询会看到两个聊天气泡。现在链接作为图片的同一消息链发出
  （`Image` + `Plain`），只出现一条消息。
- **新增国内可访问的商品详情链接**：`store.steampowered.com` 在部分网络下
  无法访问，现在会同时给出小黑盒页面（可用 appid 直接拼接、免登录）：

  ```
  商品详情：https://www.xiaoheihe.cn/app/topic/game/105600
  Steam 商店：https://store.steampowered.com/app/105600/
  ```

  官方链接保留作为规范来源。实测小黑盒该页面 HTTP 200，
  三个不同 appid 均返回同一外壳（内容由浏览器 JS 渲染），
  做外链没问题。

  注：调研中候选的 SteamDB（403）、游民星空 `/app/{id}`（落到无关页面）
  实测均不可用，未采用。

## 1.3.1

修正 1.3.0 的一个判断失误：**图片里的链接是点不了的**。

1.3.0 把商店链接从灰色小字改成高亮 URL，但链接**画在图片里**，
用户仍然只能手抄 —— 比改之前更难用（灰字时至少不会误以为能点）。
现在查询游戏时会**额外发一条纯文本消息**：

```
Terraria：https://store.steampowered.com/app/105600/
```

客户端会把纯文本里的 URL 识别为可点击链接。图片上仍保留链接，
方便截图分享时保持自包含。

## 1.3.0

游戏详情卡变「信息完整」的一版。三项改动的数据本来就已经在返回里，
只是渲染层没画出来，属于纯白捡。

- **距史低差额**：以前只有「史低 ¥18」和「当前 ¥42」两个孤立数字，
  看不出到底离史低有多远。现在右侧直接显示「距史低还差 ¥24」；
  已经是史低时显示「当前已是史低」。差额是用户真正会据以决策的数字。
- **游戏简介**：`short_description` 一直有取但从未渲染。现在显示在价格区下方，
  最多 3 行，超出用省略号。卡片终于能回答「这游戏是什么」，而不只是「多少钱」。
- **商店链接更醒目**：此前是一行灰色小字，容易被当成页脚忽略。
  现在是「商店链接：」+ 高亮色的可点击 URL。
- **修复换行器的三个真实缺陷**（由新增测试发现，非本次功能引入）：
  - 英文单词会从中间断开（`aaa bbb` → `aaa bb` / `b ccc`）；
  - 换行后残留行首/行尾空格；
  - 截断时把尾部文本重复拼接（`ccc ddd` + `fff` 渲染成 `dddfff`）。
  现在按词换行（中文仍按字）、去除多余空白、截断优先落在词边界。
- 顺带修掉换行后「。」「，」单独占一行的问题。

注：`genres`（类型）字段本想一并加上，实测该接口在现有请求与
`include_genres` 下**均不返回**，故未做，避免留下永远为空的死字段。

## 1.2.0

稳定性版本：核心依赖都是非官方接口，这一版让它们在出问题时**体面降级**，
而不是把英文异常直接甩给用户。

- **小黑盒搜索兜底**：中文名转换完全依赖小黑盒，此前它一挂
  `steam游戏 泰拉瑞亚` 就直接失败。现在失败时会改用 Steam 商店搜索兜底
  （`store.steampowered.com/api/storesearch/`），英文名与 appid 照常可用；
  中文名仍无法解析时，给出明确提示并建议改用 appid，而不是抛网络错误。
- **接口自检**：启动时后台探测四个依赖（Steam 商店数据、小黑盒中文名转换、
  Steam 热门榜、Steam 在线人数），把结果写进日志并标出核心依赖。
  以后「机器人怎么不回话」可以直接从日志定位到是哪个接口挂了。
  自检在后台运行且总耗时受限（45 秒），不会拖慢插件加载。
- 自检与兜底都不影响既有功能：任一探测失败只是记日志。

## 1.1.2

- 在线人数卡片的时间戳由 UTC 改为**北京时间**（+08:00），并标注「北京时间」。
  面向国内用户时 UTC 时钟容易被当成错误时间而不是时区差异。
- 时区解析带兜底：优先用 `zoneinfo` 读取 `Asia/Shanghai`，
  读取失败（精简镜像常缺 tzdata）时回退到固定 +08:00。
  中国自 1991 年起全境统一使用 +08:00 且不实行夏令时，
  因此该回退是精确等价而非近似，两种环境下卡片完全一致。

## 1.1.1

- `steam在线` 与 `steam热度` 的卡片加上**游戏封面缩略图**，与价格卡片风格统一。
  封面通过已有的 `IStoreBrowseService/GetItems` 批量取回后并发下载；
  下载失败或缺失时画占位图，不会影响卡片渲染。
- 单个游戏的卡片改为信息卡样式：去掉「排行第 1」的序号（只有一条记录时
  这个序号会读成「1 条里的第 1 名」），封面放大，标题改为「Steam 实时在线」。

## 1.1.0

- 新增 `steam在线 <游戏名|appid>`：查询单个游戏的**实时在线人数**。
- 新增 `steam热度 [数量]`：在线人数榜，**按实时人数从高到低排序**。
- 关于数据来源的一个重要说明：Steam 的热门榜接口
  （`ISteamChartsService/GetMostPlayedGames`）返回的是当日峰值
  `peak_in_game`，**不是实时人数**。实测两者排序差异很大
  （GTA V 峰值第 8、实时第 34；Rust 峰值第 26、实时第 13），
  且比值在 0.26 ~ 1.01 之间浮动，无法换算。
  因此榜单只用来挑选候选游戏，实际排序一律使用
  `ISteamUserStats/GetNumberOfCurrentPlayers` 的实时值逐游戏查询。
  榜单的峰值作为参考信息单独标注，不会当作实时人数展示。
- 实时人数需要逐游戏请求，因此并发上限设为 12；实测 20 个约 2 秒，
  50 个约 2.4 秒。默认显示 20 个，可用 `max_players` 配置。
- 新增命令别名：`steam在线人数`/`steam人数`、`steam热度榜`/`steam排行`/`steam在线榜`。

## 1.0.5

- 修复图片发不出去：命令已命中、图片也已生成，但发送阶段报
  `OSError: [Errno 36] File name too long: '/AstrBot/base64:/iVBOR...'`。
  原因是 `event.image_result()` 会把参数当作媒体地址去解析，
  `base64://` 被当成本地路径。改为自行构造
  `Comp.Image(file="base64://…")` 并走 `chain_result`，
  与同环境其它插件验证过的写法一致。

## 1.0.4

- 修复 `steam打折` 在国内服务器上间歇性失败：特惠列表所在主机是**时通时断**
  （实测 6 次里只成功 2 次），并非完全不可达。
  改为每次尝试使用 6 秒短超时并最多重试 5 次，实测 5 轮全部成功，
  耗时 0.8 ~ 13 秒。
- 说明：中国区商店 `store.steamchina.com` 也有同名的特惠接口且始终可用，
  但它返回的是「蒸汽平台」独立目录（实测仅 37 款且均为国产游戏），
  不是完整 Steam 目录，因此未采用。

## 1.0.3

- 封面图改为候选链：按「接口给出的文件名 → Steam 固定路径」依次尝试，
  个别游戏封面不在标准路径时会自动回退，不再显示占位图。
- 首次请求的探测超时收紧到 8 秒，避免为不可达的 API 主机白等满整个超时。
- 补充故障转移、候选链与超时策略的回归测试。

## 1.0.2

- 修复国内服务器无法查询的问题：`api.steampowered.com` 在境内不可达，
  改为在 `api.steampowered.com` 与 `api.steamchina.com` 之间自动故障转移，
  并记住可用主机，避免每次查询重复等待连接超时。
- 中国区接口不返回 `main_capsule` / `header` 文件名，改为按 appid 推导封面图。
- 错误信息补上异常类型名，此前 httpx 超时异常字符串为空，
  用户只能看到以冒号结尾的残缺提示。

## 1.0.1

- 修复裸命令无响应：AstrBot 的 `CommandFilter` 要求消息带唤醒前缀或 @机器人，
  而本插件原先使用命令过滤器，导致直接发送 `steam游戏 泰拉瑞亚` 被静默忽略。
  改用不受唤醒前缀限制的 `RegexFilter`，并自行解析命令与参数。
- 移除 `GreedyStr` 依赖。

## 1.0.0

- 首次发布。
- `steam游戏`：以图片查询 Steam 游戏当前价、原价与折扣、史低、评价和商店封面。
- `steam打折`：以图片列出当前促销游戏，含展示图、游戏名、当前价、史低和折扣结束日期。
- 通过小黑盒搜索接口把中文游戏名转换为 Steam appid，无需 LLM 或 API Key。
- 支持 appid 与 Steam 商店链接直接查询。
- 名称不唯一时返回编号候选图片，回复序号即可选择。
